Import('RTT_ROOT')
Import('rtconfig')
from building import *

cwd     = os.path.join(str(Dir('#')), 'Platforms')
src	= Glob('*.c')
src += ['lowlevel.S']
CPPPATH = [cwd]

group = DefineGroup('Platforms', src, depend = [''], CPPPATH = CPPPATH)

Return('group')
